อ่าน AGENTS.md + PROGRESS.md — เริ่ม Phase 5

### 5.1 Cloud Sync (Supabase free tier)
- SQLite ในเครื่องยังเป็น source of truth — cloud เป็นแค่กระจกสำหรับ sync ข้ามเครื่อง
- **Offline-first เต็มรูปแบบ:** บันทึกลงเครื่องก่อนเสมอ เข้าคิวไว้ sync ทีหลัง
- Sync แบบ delta ด้วย updated_at + sync_version
- Conflict: last-write-wins (ผู้ใช้คนเดียว) แต่เก็บ log ไว้ให้ดูย้อนหลังได้
- ปุ่ม "sync ตอนนี้" + แสดงเวลา sync ล่าสุด + สถานะ (กี่รายการรอ sync)
- ตั้งค่าได้ว่า sync อัตโนมัติเมื่อต่อ Wi-Fi เท่านั้น
- **ถ้า sync ล้มเหลว ห้ามทำให้ข้อมูลในเครื่องเสียหายเด็ดขาด**

### 5.2 นำเข้าข้อมูลจาก Notion
- Import wizard รับไฟล์ CSV
- **หน้า mapping คอลัมน์:** ให้ผู้ใช้ลากจับคู่คอลัมน์ CSV → ฟิลด์ในระบบ บันทึก mapping ไว้ใช้ซ้ำได้
- **Preview ก่อน import จริง** แสดง 20 แถวแรก + รายการที่จะ error
- ตรวจจับรายการซ้ำ, สร้างบัญชี/หมวดที่ยังไม่มีให้อัตโนมัติ (ถามก่อน)
- **Rollback ได้ทั้งชุด** ถ้า import แล้วไม่ถูกใจ
- รองรับรูปแบบวันที่และตัวเลขแบบไทย

### 5.3 เก็บงาน
- ตรวจ performance: เปิดแอปต้องไม่เกิน 2 วินาที แม้มีข้อมูล 20,000 รายการ
- เพิ่ม index ที่จำเป็นทั้งหมด
- เขียน `docs/USER_MANUAL_TH.md` คู่มือใช้งานภาษาไทยพร้อมภาพประกอบ