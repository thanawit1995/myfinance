# AGENTS.md — กฎประจำโปรเจกต์ (อ่านทุกครั้งก่อนทำงาน)

## 1. บริบทผู้ใช้ (สำคัญที่สุด)
- ผู้ใช้ **เขียนโค้ดไม่เป็นเลย** ไม่เคยใช้ terminal มาก่อน
- ใช้ Windows 11 เป็นเครื่องหลัก, มือถือ Android, tablet ไว้ดูรายงาน
- มีเวลาสัปดาห์ละ ~5 ชั่วโมง
- **ห้ามสมมติว่าผู้ใช้รู้ศัพท์เทคนิค** ทุกครั้งที่พูดถึง build / migration / package
  ต้องอธิบายว่าคืออะไรในหนึ่งประโยคภาษาไทย

## 2. กฎการสื่อสาร
- ตอบผู้ใช้เป็น **ภาษาไทย** เสมอ (ศัพท์เทคนิคคงภาษาอังกฤษได้)
- คำสั่งที่ต้องรัน ให้ใส่ code block แยกทีละคำสั่ง + บอกว่า
  "เปิด PowerShell แล้ววางบรรทัดนี้" ระบุ path ให้ชัด
- ทุกครั้งที่จบงาน ให้สรุปท้ายด้วย 3 หัวข้อนี้เสมอ:
  1. **ทำอะไรไปบ้าง** (bullet สั้นๆ)
  2. **สิ่งที่คุณต้องทำเอง** (ถ้าไม่มี ให้เขียนว่า "ไม่มี")
  3. **วิธีทดสอบว่าใช้ได้จริง** (step-by-step กดตรงไหน คาดหวังเห็นอะไร)

## 3. กฎคุณภาพโค้ด (ห้ามละเมิด)
- **ห้ามทิ้ง `TODO`, `FIXME`, mock data หรือฟังก์ชันเปล่า** ทุกฟีเจอร์ต้องทำงานได้จริง
- ทุก business logic (FIFO, ภาษี, รอบบัตรเครดิต, สุขภาพการเงิน) ต้องมี **unit test**
  และต้องรัน `flutter test` ผ่านทั้งหมดก่อนบอกว่าเสร็จ
- `flutter analyze` ต้อง 0 error, 0 warning
- ถ้าแก้แล้วพัง ให้ย้อนกลับและลองวิธีอื่น **ห้ามส่งมอบโค้ดที่รันไม่ได้**

## 4. กฎเรื่องเงิน (ห้ามละเมิดเด็ดขาด)
- เก็บจำนวนเงินเป็น **integer หน่วยสตางค์** (`int`) เท่านั้น — ห้ามใช้ `double`/`float`
- จำนวนหน่วยสินทรัพย์ (หุ้น/คริปโต/ทอง) ใช้ `Decimal` ความละเอียด 8 ตำแหน่ง
- อัตราแลกเปลี่ยนใช้ `Decimal` ความละเอียด 6 ตำแหน่ง
- ทุกธุรกรรมที่ไม่ใช่ THB **ต้องเก็บทั้ง 3 ค่า**: amount_original, fx_rate, amount_thb
  ห้ามคำนวณ THB ย้อนหลังจากเรตปัจจุบันเด็ดขาด (ต้อง lock เรต ณ วันทำรายการ)

## 5. กฎ Schema
- Primary key = **UUID v4** (string) ไม่ใช่ auto-increment
- ทุกตารางมี: `created_at`, `updated_at`, `deleted_at` (soft delete), `sync_version` (int)
- **ห้ามสร้างตาราง/คอลัมน์แยกตามเดือนหรือปีเด็ดขาด** — เวลาเป็นข้อมูลในแถว
  ทุกการสรุปรายเดือน/รายปีต้องมาจาก SQL query หรือ VIEW เท่านั้น
- `transactions` เป็น append-only ledger: แก้ไขได้ แต่ต้องบันทึกลง `audit_log`
- ทุกการเปลี่ยน schema ต้องเขียน migration พร้อม rollback

## 6. กฎภาษาในแอป
- รองรับไทย + อังกฤษ สลับได้ใน Settings (ค่าเริ่มต้น: ไทย)
- ใช้ `flutter_localizations` + ไฟล์ ARB — **ห้าม hardcode ข้อความในโค้ด UI**
- แสดงปีเป็น **ค.ศ. (CE)** ทุกที่ ยกเว้นหน้ารายงานภาษีที่ให้แสดงทั้ง ค.ศ. และ พ.ศ.
- ฟอนต์: Noto Sans Thai (ต้องอ่านภาษาไทยได้สวยทั้ง Windows และ Android)

## 7. กฎกระบวนการทำงาน
- ก่อนเขียนโค้ด ให้สร้าง **Implementation Plan artifact** ก่อนเสมอ แล้วรอผู้ใช้อนุมัติ
- ถ้ามีจุดที่ต้องตัดสินใจแล้วกระทบ schema หรือ business logic → **หยุดถามก่อน ห้ามเดา**
- ทำทีละ Phase ห้ามข้ามไป Phase ถัดไปเอง
- หลังจบแต่ละ Phase ให้อัปเดต `PROGRESS.md` (ทำอะไรไปแล้ว / เหลืออะไร / รู้ปัญหาอะไร)

## 8. Tech stack (ล็อกแล้ว ห้ามเปลี่ยนโดยไม่ถาม)
Flutter 3.x / Dart 3.x · drift (SQLite) · riverpod · go_router · decimal
· home_widget · fl_chart · excel · pdf+printing · intl · flutter_secure_storage
Target: Windows desktop + Android (minSdk 26). **ไม่ต้องทำ iOS/macOS/Web**